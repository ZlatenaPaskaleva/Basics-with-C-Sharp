string name = Console.ReadLine();
int numOfProjects = int.Parse(Console.ReadLine());

int totalNeededTime = numOfProjects * 3;

Console.WriteLine($"The architect {name} will need {totalNeededTime} hours to complete {numOfProjects} project/s.");
